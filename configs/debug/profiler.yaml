# @package _global_
# original refer to: https://github.com/ashleve/lightning-hydra-template/blob/main/configs/debug/profiler.yaml

# runs with execution time profiling

defaults:
  - default

trainer:
  max_epochs: 1
  profiler: "simple"
  # profiler: "advanced"
  # profiler: "pytorch"
