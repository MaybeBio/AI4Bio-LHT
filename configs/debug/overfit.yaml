# @package _global_
# original refer to: https://github.com/ashleve/lightning-hydra-template/blob/main/configs/debug/overfit.yaml

# overfits to 3 batches

defaults:
  - default

trainer:
  max_epochs: 20
  overfit_batches: 3

# model ckpt and early stopping need to be disabled during overfitting
callbacks: null
